[project]
name = "nfn-mvp"
version = "0.1.0"
description = "NFN MVP monorepo with service-based backend and Kotlin Android mobile app."
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["backend/tests"]
pythonpath = ["."]

